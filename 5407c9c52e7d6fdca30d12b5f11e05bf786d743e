problem: Check whether docs were up to date for FreqAI Lopez de Prado PR
approach: Compared prediction model classes and LDP feature/config keys against docs, added mkdocs nav entry and parameter-table rows for LDP settings
failed_paths:
  - mkdocs build could not run locally because mkdocs is not installed
verification:
  - git diff --check
  - coverage script confirmed all LDP model classes documented, all LDP config keys in docs/freqai-lopez-de-prado.md and docs/freqai-parameter-table.md, and nav includes freqai-lopez-de-prado.md
workflow_learning: For docs completeness, check both dedicated docs and the project nav/parameter table; a dedicated doc alone is not discoverable enough
related_files:
  - mkdocs.yml
  - docs/freqai-parameter-table.md
  - docs/freqai-lopez-de-prado.md
